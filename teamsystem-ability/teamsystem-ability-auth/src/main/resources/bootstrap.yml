spring:
  application:
    name: oauth-auth
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        file-extension: yaml
  profiles:
    active: "@runtime-profile@"

server:
  port: 8100



